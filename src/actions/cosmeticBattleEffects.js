// APPLY_HIGHLIGHT

export const applyHighlight = (target) => ({
	type: 'APPLY_HIGHLIGHT',
	target,
});

// APPLY_IS_ACTIVE

export const applyIsActive = (target) => ({
	type: 'APPLY_IS_ACTIVE',
	target,
});

// APPLY_SHAKING

export const applyShaking = (target) => ({
	type: 'APPLY_SHAKING',
	target,
});

// CLEAR_SHAKING

export const clearShaking = (target) => ({
	type: 'CLEAR_SHAKING',
	target,
});

// APPLY_PULSING

export const applyPulsing = (target) => ({
	type: 'APPLY_PULSING',
	target,
});

// CLEAR_SHAKING

export const clearPulsing = (target) => ({
	type: 'CLEAR_PULSING',
	target,
});

// CLEAR_ALL_COSMETIC_EFFECTS
export const clearAllCosmeticEffects = (target) => ({
	type: 'CLEAR_ALL_COSMETIC_EFFECTS',
	target,
});
